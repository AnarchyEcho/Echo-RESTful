# Shopping list RESTful API created in NodeJS with TypeScript

The purpose of this API is for a shopping list phone app.

Optional: Docker commands: [docker build -t NAME . , docker run -p PORT:PORT NAME]
